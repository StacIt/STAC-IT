import type React from "react"
import { useState, useRef } from "react"
import {
    View,
    TouchableOpacity,
    StyleSheet,
    Modal,
    ScrollView,
    Alert,
    Keyboard,
    TouchableWithoutFeedback,
    KeyboardAvoidingView,
    Platform,
    ActivityIndicator,
} from "react-native"
import {
    Button,
    Card,
    Divider,
    List,
    Text,
    TextInput,
    useTheme
} from "react-native-paper"

const StacOptions = () => {}

export default StacOptions
